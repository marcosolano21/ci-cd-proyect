services:
  flask:
    image: marcosolano/flask:latest
    container_name: flask-app

  nginx:
    image: marcosolano/nginx:latest
    container_name: nginx
    ports:
      - "80:80" # Maping port 80 to port 80 on the container
    depends_on:
      - flask